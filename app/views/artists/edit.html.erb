<h1><%= "Edit the information for #{@artist.name} below:" %></h1>

<%= form_for @artist do |f| %>
  <%=f.label :name %>
  <%= f.text_field :name %>
  <%=f.label :bio %>
  <%=f.text_area :bio %>
  <%= f.submit "Update Artist" %>
<% end %>