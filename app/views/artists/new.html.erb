<h1>This is the form for a new Artist:</h1>

<%= form_for @artist do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :bio %>
  <%= f.text_area :bio %>
  <%= f.submit "Create Artist" %>
<% end %>
