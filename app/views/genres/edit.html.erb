<h1><%= "Edit your #{@genre.name} here:" %></h1>

<%= form_for @genre do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :info %>
  <%= f.text_field :info %>
  <%= f.submit "Update Genre" %>
<% end %>